import java.util.ArrayList;
import java.util.Scanner;

class Course extends Admin {
		
	String name;
	String courseID;
	
	public Course(String name, String courseID) {
		this.name = name;
		this.courseID = courseID;
	}
	// I put one if statement and for loop in here I don't know which one we need 
	// Whichever one works 

    public void Print(){
        System.out.print(("("+getName()));
        System.out.print(",");
        System.out.print(getid());
        System.out.print(") ");
    }

	private String getid() {
		// TODO Auto-generated method stub
		return (courseID);
	}

	private String getName() {
		// TODO Auto-generated method stub
		return (name);
	}

	/*public void createCourse(String name, String CourseID, int i) {
		// creating a menu for for create course

		for (creationCourse = true, i = 0; i > 0; i++) {
			System.out.println("Create Course");

			System.out.println("\n -----------------------------");

			// Getting input (dunno if this is the right way, wanna be able to make the
			// input into the array)
			Scanner s = new Scanner(System.in);

			System.out.println("Enter The course name:");
			this.name = s.nextLine();
			System.out.println("Enter Course Code");
			this.courseID = s.nextLine();
			System.out.println("Course Created");

		}
	}
	/*
	 * if (creationCourse == true) { System.out.println("Create Course");
	 * 
	 * System.out.println("\n -----------------------------");
	 * 
	 * // Getting input (dunno if this is the right way, wanna be able to make the
	 * // input into the array) Scanner s = new Scanner(System.in);
	 * 
	 * System.out.println("Enter The course name:"); this.name = s.nextLine();
	 * System.out.println("Enter Course Code"); this.courseID = s.nextLine();
	 * System.out.println("Course Created");
	 * 
	 * }
	 */
	// one to many association with course offerings
	// ArrayList<CourseOffering> co = new ArrayList<CourseOffering>();

}
